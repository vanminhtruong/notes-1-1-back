# Production Environment Configuration for Render

# Server Configuration
NODE_ENV=production
PORT=10000

# Database
SQLITE_STORAGE=/opt/render/project/src/data/app.sqlite

# Logging
SEQUELIZE_LOG=false

# JWT Configuration (will be set via Render environment variables)
# JWT_SECRET=
# JWT_EXPIRES_IN=7d

# Client Configuration (will be set via Render environment variables)
# CLIENT_URL=
# CORS_ORIGIN=

# SMTP Configuration (optional for production)
SMTP_HOST=
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=
SMTP_FROM="Plant App <no-reply@plantapp.com>"
